"use client";

import React from "react";
import Link from "next/link";
import { 
  Trophy, 
  TrendingUp, 
  ChevronRight, 
  CheckCircle2, 
  AlertCircle,
  Lightbulb,
  FileText,
  Map
} from "lucide-react";
import { DashboardLayout } from "@/components/layouts/dashboard-layout";
import { Card, CardContent, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";

export default function DiagnosticResultPage() {
  // Mock Diagnostic Data
  const results = {
    overallBand: 6.5,
    targetBand: 7.5,
    skills: [
      { name: "Grammatical Range", score: 6.0, color: "bg-blue-500" },
      { name: "Lexical Resource", score: 7.0, color: "bg-teal-500" },
      { name: "Coherence & Cohesion", score: 6.5, color: "bg-purple-500" },
      { name: "Fluency & Pronunciation", score: 6.0, color: "bg-orange-500" },
    ],
    strengths: ["Task Response", "Vocabulary Variety"],
    weaknesses: ["Complex Sentence Structures", "Filler Words in Speaking"]
  };

  return (
    <DashboardLayout>
      <div className="max-w-5xl mx-auto space-y-8 pb-12">
        
        {/* Header Summary */}
        <div className="flex flex-col md:flex-row md:items-end justify-between gap-4">
          <div>
            <Badge variant="success" className="mb-2">Assessment Complete</Badge>
            <h1 className="text-3xl font-bold tracking-tight">Your Diagnostic Report</h1>
            <p className="text-muted-foreground">Generated by AI Coach based on your 20-minute baseline test.</p>
          </div>
          <Link href="/roadmap">
            <Button className="bg-accent hover:bg-accent/90 text-accent-foreground shadow-lg h-12">
              Generate My Study Roadmap <Map className="ml-2 h-5 w-5" />
            </Button>
          </Link>
        </div>

        <div className="grid gap-8 lg:grid-cols-3">
          
          {/* Main Score & Gap Analysis */}
          <div className="lg:col-span-2 space-y-8">
            <Card className="bg-gradient-to-br from-primary to-blue-700 text-white border-none shadow-xl">
              <CardContent className="pt-10 pb-10 flex flex-col items-center text-center">
                <div className="relative">
                  <div className="h-32 w-32 rounded-full border-4 border-white/20 flex items-center justify-center">
                    <span className="text-5xl font-black">{results.overallBand}</span>
                  </div>
                  <Trophy className="absolute -top-2 -right-2 h-10 w-10 text-warning" />
                </div>
                <h2 className="mt-6 text-2xl font-bold uppercase tracking-widest">Current Band Level</h2>
                <p className="mt-2 text-primary-foreground/80 max-w-sm">
                  You are performing at a **Competent User** level (CEFR B2). 
                  You are 1.0 band point away from your goal.
                </p>
                
                <div className="mt-8 w-full max-w-md bg-white/10 rounded-2xl p-4 flex items-center justify-around">
                  <div className="text-center">
                    <p className="text-xs text-white/60 uppercase">Current</p>
                    <p className="text-xl font-bold">{results.overallBand}</p>
                  </div>
                  <TrendingUp className="h-6 w-6 text-accent" />
                  <div className="text-center">
                    <p className="text-xs text-white/60 uppercase">Target</p>
                    <p className="text-xl font-bold">{results.targetBand}</p>
                  </div>
                </div>
              </CardContent>
            </Card>

            {/* Detailed Skill Breakdown */}
            <Card>
              <CardHeader>
                <CardTitle>Skill Breakdown</CardTitle>
                <CardDescription>Performance across official IELTS marking criteria.</CardDescription>
              </CardHeader>
              <CardContent className="space-y-6">
                {results.skills.map((skill) => (
                  <div key={skill.name} className="space-y-2">
                    <div className="flex justify-between text-sm">
                      <span className="font-medium">{skill.name}</span>
                      <span className="font-bold">Band {skill.score}</span>
                    </div>
                    <div className="h-3 w-full bg-secondary rounded-full overflow-hidden">
                      <div 
                        className={`h-full ${skill.color} transition-all duration-1000`} 
                        style={{ width: `${(skill.score / 9) * 100}%` }}
                      />
                    </div>
                  </div>
                ))}
              </CardContent>
            </Card>
          </div>

          {/* Insights & Recommendations */}
          <div className="space-y-6">
            <Card>
              <CardHeader>
                <CardTitle className="text-lg flex items-center gap-2">
                  <CheckCircle2 className="h-5 w-5 text-success" /> Key Strengths
                </CardTitle>
              </CardHeader>
              <CardContent>
                <ul className="space-y-3">
                  {results.strengths.map(s => (
                    <li key={s} className="text-sm flex items-center gap-2 text-muted-foreground">
                      <span className="h-1.5 w-1.5 rounded-full bg-success" /> {s}
                    </li>
                  ))}
                </ul>
              </CardContent>
            </Card>

            <Card>
              <CardHeader>
                <CardTitle className="text-lg flex items-center gap-2">
                  <AlertCircle className="h-5 w-5 text-error" /> Areas for Growth
                </CardTitle>
              </CardHeader>
              <CardContent>
                <ul className="space-y-3">
                  {results.weaknesses.map(w => (
                    <li key={w} className="text-sm flex items-center gap-2 text-muted-foreground">
                      <span className="h-1.5 w-1.5 rounded-full bg-error" /> {w}
                    </li>
                  ))}
                </ul>
              </CardContent>
            </Card>

            <Card className="bg-accent/5 border-accent/20">
              <CardHeader>
                <CardTitle className="text-lg flex items-center gap-2 text-accent">
                  <Lightbulb className="h-5 w-5" /> AI Tip
                </CardTitle>
              </CardHeader>
              <CardContent>
                <p className="text-sm text-muted-foreground leading-relaxed">
                  Focus your first week on **Grammatical Range**. Improving your use of 
                  conditional sentences could boost your writing score by 0.5 points alone.
                </p>
                <Button variant="link" className="px-0 mt-2 text-accent h-auto">
                  Learn how <ChevronRight className="h-4 w-4" />
                </Button>
              </CardContent>
            </Card>
          </div>

        </div>

        {/* Action Footer */}
        <div className="flex flex-col items-center gap-4 py-6 border-t border-border mt-8">
          <p className="text-sm text-muted-foreground">Want to dive deeper into your mistakes?</p>
          <div className="flex gap-4">
            <Button variant="outline">
              <FileText className="mr-2 h-4 w-4" /> Download PDF Report
            </Button>
            <Link href="/roadmap">
              <Button>Next: Build My Study Plan</Button>
            </Link>
          </div>
        </div>

      </div>
    </DashboardLayout>
  );
}